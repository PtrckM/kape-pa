//
//  ContentView.swift
//  kape pa
//
//  Created by PtrckM on 12/16/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        // This view is not used since we're using a menu bar app
        // but we need to keep the file for the project structure
        EmptyView()
    }
}

#Preview {
    ContentView()
}
